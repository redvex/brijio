{
  "name": "@browserbridge/chrome-extension",
  "version": "0.0.0",
  "private": true,
  "description": "BrowserBridge Chrome extension.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && cp manifest.json dist/manifest.json && cp src/setup.html dist/setup.html",
    "check": "tsc --noEmit",
    "dev": "echo \"Chrome extension dev workflow is not implemented yet\"",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@browserbridge/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
