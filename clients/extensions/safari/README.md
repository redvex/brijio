# BrowserBridge Safari Extension

Safari support is planned but not implemented.

This folder exists to reserve the client boundary for a future Safari extension.
Safari packaging, permissions, native app requirements, and user-control
behavior should be designed in a separate ADR before implementation starts.
