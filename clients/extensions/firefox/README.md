# Brijio Firefox Extension

Firefox support is planned but not implemented.

This folder exists to reserve the client boundary for a future Firefox
extension. Firefox packaging, permissions, and user-control behavior should be
designed in a separate ADR before implementation starts.
